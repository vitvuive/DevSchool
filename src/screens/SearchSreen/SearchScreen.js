import React, { Component, } from 'react';
import { View, Text, } from 'react-native';
export default class SearchScreen extends Component {
  state = {};
  render() {
    return (
      <View>
        <Text>vier dep trai Search Screen</Text>
      </View>
    );
  }
}
